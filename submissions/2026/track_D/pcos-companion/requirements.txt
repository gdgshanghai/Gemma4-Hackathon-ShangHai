# —— 服务层 ——
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
python-multipart==0.0.9

# —— 隐私 / 加密 ——
cryptography==43.0.1

# —— 测试 ——
pytest==8.3.3

# —— 模型推理后端（按需安装其一；详见 README）——
# 端侧 Gemma 4 4B：使用 Ollama（独立安装，非 pip）
# 云端 Gemma 4 26B MoE 多模态：vllm==0.6.* （需 CUDA，建议单独环境）
