# Project Instructions

禁止批量删除文件或目录。

不要使用 `rm -rf`、`Remove-Item -Recurse` 或同类递归删除命令。
任何删除文件的操作都必须先向用户确认。

当需求、目标、边界或背景信息不明确，导致无法可靠判断时，应先与用户确认。

医疗相关功能必须遵守以下边界：

- 不做 HIV 确诊或排除诊断。
- 不输出个体化药物、停药或换药建议。
- 风险评估仅用于分流和就医准备。
- 危机表达优先展示 12356、110、120 等求助行动。
