# Utility scripts — placeholder.
#
# Common development and operations scripts:
#   - bootstrap-dev.sh     — Initialize local dev environment (kind cluster, dependencies)
#   - gen-proto.sh         — Run buf generate to produce protobuf stubs
#   - seed-fixtures.sh     — Load synthetic EEG fixtures into feature-store
#   - e2e-test.sh          — Run the full local end-to-end test suite
