apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Staging overlay — placeholder.
# Should mirror prod but with staging-specific secrets, endpoints, and replica counts.

bases:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging
