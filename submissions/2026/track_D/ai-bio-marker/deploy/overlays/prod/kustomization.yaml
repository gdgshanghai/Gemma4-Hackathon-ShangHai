apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production overlay — placeholder.
# Should enable HA, GPU node selectors, resource quotas, and image signing verification.

bases:
  - ../../base

namePrefix: prod-

commonLabels:
  environment: production
