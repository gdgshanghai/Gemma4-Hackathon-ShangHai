# fl-aggregator — Python 3.12 slim
# Federated learning weight aggregator (FedAvg).
# Raw patient data never leaves the origin domain; only weight deltas are exchanged.

FROM python:3.12-slim
RUN useradd -m -u 1000 cloud
USER cloud
WORKDIR /app
COPY main.py .
ENTRYPOINT ["python", "main.py"]
