# Data schemas — placeholder.
#
# Versioned data contracts for persistent storage (object store, time-series DB, Postgres).
#
# These schemas define the on-disk / in-DB format, distinct from the wire-format
# protobuf contracts in proto/.
