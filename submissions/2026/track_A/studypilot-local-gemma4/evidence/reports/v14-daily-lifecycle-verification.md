# V14 每日晚间生命周期验证

验证日期：2026-07-16

## 结论

- 本地模型为 `gemma-4-26b-a4b-it`，状态 `loaded`，具备 `tool_use`。
- 上海自然日内只产生一条权威晚间记录；关闭后同日再次开始返回同一 ID 和 `closed` 状态。
- 次日开始前 `/evenings/today` 返回 `404`，开始后生成不同 ID；两天各一条登记记录。
- 家长按日期读取到与孩子端相同的已归档记录，不需要确认或批准。
- 模拟数据全部位于临时 SQLite，未写入真实家庭数据库。

## 自动化门禁

- `python -m pytest -q`：`936 passed, 6 deselected`。
- `python -m pytest -m real_lm -q`：`6 passed, 936 deselected`。
- `python -m ruff check .`：通过。
- 孩子端：`14` 项测试通过，生产构建通过。
- 家长端：`7` 项测试通过，生产构建通过。

## 完整一晚模拟

临时数据库中输入语文、数学、英语、生物四项自然语言作业，真实 Gemma 完成结构化盘点，随后依次完成清单确认、可行路线生成、路线提交和睡前复盘。

结果：

- 任务数：`4`。
- 规划状态：`plan_draft`，`feasible=true`。
- 关闭状态：`closed`，outcome 数量 `4`。
- 同日再次开始：ID 不变，状态仍为 `closed`。
- 家长按日期读取：ID 与孩子记录一致。
- 次日开始：生成新 ID。
- 每日登记数：`2` 天共 `2` 条。
- 重复 outcome：`0`。

## 界面核验

- 孩子端在 `1440x900` 和 `360x800` 下无横向溢出，今天无记录时进入“今晚盘点”。
- 家长端在桌面和 `360x800` 下均能显示历史“已归档”记录，没有审批、确认或写入按钮。
- 真实历史数据库的 7 条旧会话全部保留，2026-07-12 的权威记录为 `evening-bccc93677b27388673fbe118`。
